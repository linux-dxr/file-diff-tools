name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
<<<<<<< HEAD
  test-windows:
    runs-on: windows-latest
    strategy:
      matrix:
        python-version: ['3.9', '3.10', '3.11']
=======
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python-version: ['3.8', '3.9', '3.10', '3.11']
        exclude:
          # Reduce matrix size for faster CI
          - os: macos-latest
            python-version: '3.8'
          - os: macos-latest
            python-version: '3.9'
>>>>>>> 1aa9785be6cadf7180d6c1289a64c5870dc63e5f
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
<<<<<<< HEAD
        pip install pytest pytest-cov flake8
        
    - name: Lint with flake8
      run: |
=======
        pip install pytest pytest-cov
        
    - name: Lint with flake8
      run: |
        pip install flake8
>>>>>>> 1aa9785be6cadf7180d6c1289a64c5870dc63e5f
        # Stop the build if there are Python syntax errors or undefined names
        flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
        # Exit-zero treats all errors as warnings. The GitHub editor is 127 chars wide
        flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
        
    - name: Test with pytest
      run: |
        pytest --cov=. --cov-report=xml
        
    - name: Upload coverage to Codecov
<<<<<<< HEAD
      if: matrix.python-version == '3.11'
=======
      if: matrix.os == 'ubuntu-latest' && matrix.python-version == '3.11'
>>>>>>> 1aa9785be6cadf7180d6c1289a64c5870dc63e5f
      uses: codecov/codecov-action@v3
      with:
        file: ./coverage.xml
        flags: unittests
        name: codecov-umbrella
        
  build-test:
<<<<<<< HEAD
    runs-on: windows-latest
    needs: test-windows
=======
    runs-on: ${{ matrix.os }}
    needs: test
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
>>>>>>> 1aa9785be6cadf7180d6c1289a64c5870dc63e5f
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pyinstaller
        
    - name: Test build with PyInstaller
      run: |
<<<<<<< HEAD
        # 使用spec文件构建
        pyinstaller FileDiffTools.spec
        
    - name: Test executable
      run: |
        # 基本测试，确保可执行文件可以运行
        echo "Testing executable..."
        if (Test-Path "dist\FileDiffTools.exe") {
          echo "Executable found at dist\FileDiffTools.exe"
          # 尝试获取可执行文件信息
          Get-ItemProperty dist\FileDiffTools.exe | Format-List
        } else {
          echo "ERROR: Executable not found!"
          exit 1
        }
      shell: powershell
=======
        pyinstaller --onefile --windowed --name="FileDiffTools" main.py
        
    - name: Test executable
      run: |
        # Basic test to ensure the executable can run without errors
        if [ "$RUNNER_OS" == "Windows" ]; then
          timeout 10s ./dist/FileDiffTools.exe --version || echo "Executable started successfully"
        else
          timeout 10s ./dist/FileDiffTools --version || echo "Executable started successfully"
        fi
      shell: bash
>>>>>>> 1aa9785be6cadf7180d6c1289a64c5870dc63e5f
